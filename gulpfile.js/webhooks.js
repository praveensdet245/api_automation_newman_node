module.exports = {
    sampleService: '<slack webhook url to which assertion count should get published>'
};
//project may contain multiple service and each service may handled by different person/team, 
// when api suite runs total assertion count and failed count will get published to respective channels